'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = constantsHub;

var _Constants = require('./Constants');

var _Constants2 = _interopRequireDefault(_Constants);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _typeof(obj) { return obj && typeof Symbol !== "undefined" && obj.constructor === Symbol ? "symbol" : typeof obj; }

/**
 * high order function to manage the whole constants
 * @return {function} function to generate namespaced constants
 */
function constantsHub() {
  var hub = {};
  return function (namespace) {
    if (typeof namespace !== 'string') {
      throw new Error('Expect namespace to be string, but accept type ' + (typeof namespace === 'undefined' ? 'undefined' : _typeof(namespace)));
    }
    var constants = hub[namespace];
    if (typeof constants === 'undefined') {
      hub[namespace] = constants = new _Constants2.default(namespace);
    }
    return constants;
  };
}
module.exports = exports['default'];