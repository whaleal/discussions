'use strict';

var _fnUtils = require('../fnUtils');

var _should = require('should');

var _should2 = _interopRequireDefault(_should);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _typeof(obj) { return obj && typeof Symbol !== "undefined" && obj.constructor === Symbol ? "symbol" : typeof obj; }

describe('fnUtils', function () {
  it('get different value in every invoke', function () {
    var tmp = [];
    var base64Encoded = (0, _fnUtils.randomBase64)(0, 20);
    for (var i = 0; i < 1000; i++) {
      tmp.push((0, _fnUtils.randomBase64)());
    }
    var index = tmp.indexOf(base64Encoded);
    (0, _should2.default)(index).lessThan(0);
  });

  it('fnUtils should return 5 chars default', function () {
    var base64Encoded = (0, _fnUtils.randomBase64)();
    (0, _should2.default)(base64Encoded.length).equal(5);
  });

  it('should return spec number by spec param', function () {
    var base64Encoded = (0, _fnUtils.randomBase64)(0, 6);
    (0, _should2.default)(base64Encoded.length).equal(6);
  });

  it('should return no more than 20 chars', function () {
    var base64Encoded = (0, _fnUtils.randomBase64)(0);
    (0, _should2.default)(base64Encoded.length).belowOrEqual(20);
  });
  it('should return correct string in browser', function () {
    delete global.bota;
    var base64Encoded = (0, _fnUtils.randomBase64)(0);
    (0, _should2.default)(typeof base64Encoded === 'undefined' ? 'undefined' : _typeof(base64Encoded)).equal('string');
  });
});