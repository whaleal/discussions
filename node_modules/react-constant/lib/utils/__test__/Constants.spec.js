'use strict';

var _Constants = require('../Constants');

var _Constants2 = _interopRequireDefault(_Constants);

var _should = require('should');

var _should2 = _interopRequireDefault(_should);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('Constants', function () {
  var constants = undefined;
  before(function () {
    constants = new _Constants2.default('namespace');
  });
  it('Constants generate a constant, and the same value will be return when the method is called again.', function () {
    var c1 = constants.of('randomString');
    var c2 = constants.of('randomString');
    (0, _should2.default)(c1).equal(c2);
  });
  it('Constants should start with namespace and key world', function () {
    var c = constants.of('randomString');
    (0, _should2.default)(c).startWith('namespace-randomString');
  });
  after(function () {
    constants = null;
  });
});