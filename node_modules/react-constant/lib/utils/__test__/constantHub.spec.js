'use strict';

var _constantHub = require('../constantHub');

var _constantHub2 = _interopRequireDefault(_constantHub);

var _should = require('should');

var _should2 = _interopRequireDefault(_should);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('constantsHub', function () {
  var hub = undefined;
  before(function () {
    hub = (0, _constantHub2.default)();
  });
  it('call twice with same namespace, should return same object', function () {
    var ns1 = hub('ns1');
    var ns2 = hub('ns1');
    var c1 = ns1.of('key');
    var c2 = ns2.of('key');
    _should2.default.equal(c1, c2);
  });
  it('constant should start width namespace-key', function () {
    var ns = hub('namespace');
    var c1 = ns.of('key1');
    (0, _should2.default)(c1).startWith('namespace-key1');
    var c2 = ns.of('key2');
    (0, _should2.default)(c2).startWith('namespace-key2');
  });
  after(function () {
    hub = null;
  });
});