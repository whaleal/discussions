'use strict';

var _index = require('../index');

var _index2 = _interopRequireDefault(_index);

var _should = require('should');

var _should2 = _interopRequireDefault(_should);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('Constant', function () {
  var constants1 = undefined,
      constants2 = undefined;
  before(function () {
    constants1 = (0, _index2.default)('namespace1');
    constants2 = (0, _index2.default)('namespace2');
  });
  it('same namespace with same key should have same value', function () {
    var frokConstant = (0, _index2.default)('namespace1');
    _should2.default.equal(constants1.of('randomString'), frokConstant.of('randomString'));
  });
  it('different namespace with same key should have various values', function () {
    _should2.default.notEqual(constants1.of('randomString'), constants2.of('randomString'));
  });
  it('same namespace with different key should have various values', function () {
    _should2.default.notEqual(constants1.of('randomString1'), constants1.of('randomString2'));
  });
  it('after contant is generate, the same value can be access with . operation', function () {
    var c1 = constants1.of('randomString');
    var c2 = constants1.randomString;
    _should2.default.equal(c1, c2);
  });
});